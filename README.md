## FEFUS — сонный компаньон 🦦

**Кратко:** Telegram-бот на `aiogram 3` с виртуальной выдрой-питомцем, который помогает выстраивать здоровые привычки сна, воды, еды и активности.

### Запуск

1. Создай файл `.env` в корне проекта (см. `.env.example`):

```bash
cp .env.example .env
# отредактируй .env и подставь BOT_TOKEN=твой_telegram_bot_token
```

2. Установи зависимости (желательно в виртуальное окружение):

```bash
pip install -r requirements.txt
```

3. Запусти бота:

```bash
python main.py
```

### Основные функции

#### Для пользователей:

- **Управление выдрой:**
  - Разбудить/уложить спать (с автоматическим учётом продолжительности сна)
  - Кормить (завтрак, обед, ужин)
  - Давать воду
  - Отправлять на работу (заработок монет)
  - Заниматься хобби (покупка и использование)

- **Команды:**
  - `/start` — создать выдру или вернуться в бота
  - `/settings` — настройки (имя, часовой пояс)
  - `/set_name НовоеИмя` — изменить имя выдры
  - `/set_timezone Region/City` — изменить часовой пояс (по умолчанию Asia/Vladivostok)
  - `/pet_status` — показать состояние выдры (счастье, энергия, сытость, вода, монеты, возраст)
  - `/my_stats` — показать статистику (сон, кормления, работа, хобби)
  - `/buy_hobby id` — купить хобби за монеты
  - `/revive` — воскресить выдру (1 раз бесплатно, далее через подписку на канал)
  - `/work_together` — присоединиться к совместной работе
  - `/lunch_together` — присоединиться к совместному обеду

- **Напоминания:**
  - Автоматические напоминания 4 раза в день (10:00, 13:00, 17:00, 20:00) в часовом поясе пользователя
  - Дружелюбные тексты от выдры с предложениями совместных действий

#### Для администраторов:

- **Команды:**
  - `/admin` — панель администратора
  - `/set_channel @username` — указать канал для проверки подписки (для воскрешения)
  - `/broadcast текст` — рассылка всем пользователям
  - `/add_hobby id|Название|цена|avatar_key` — добавить новое хобби
  - `/list_hobbies` — показать все хобби
  - `/stats` — показать инфографику статистики всех пользователей

### Механика выдры

- **Состояния:** счастье, энергия, сытость, вода (0-100)
- **Экономика:** заработок монет на работе, трата на хобби
- **Возраст:** автоматически увеличивается раз в день
- **Смерть и воскрешение:**
  - Выдра умирает при критически низких показателях
  - 1 бесплатное воскрешение
  - Дальнейшие воскрешения требуют подписки на канал, указанный администратором

### Структура ассетов выдры

Временные заглушки для изображений находятся в `bot/assets/avatars/otter/`:

- `awake.txt` — выдра бодрствует (будущее `awake.png`)
- `sleep.txt` — выдра спит (будущее `sleep.png`)
- `work.txt` — выдра на работе (будущее `work.png`)
- `eat.txt` — выдра ест (будущее `eat.png`)
- `hobby.txt` — выдра занимается хобби (будущее `hobby.png`)

Когда будут готовы настоящие PNG:

- замени `.txt`-заглушки одноимёнными `.png`-файлами;
- при необходимости можно будет расширить список состояний и папок.

### Структура проекта

```
FEFUsleep/
├── main.py                 # Точка входа, регистрация хендлеров
├── settings.py             # Загрузка конфигурации из .env
├── .env.example            # Шаблон для .env (скопируй в .env)
├── .gitignore
├── bot/
│   ├── core/
│   │   ├── config.py       # Конфигурация (токен, часовой пояс по умолчанию)
│   │   ├── models.py       # Модели данных (PetState, UserState, Hobby и т.д.)
│   │   ├── repositories.py # Репозитории для работы с данными
│   │   ├── admin_handlers.py # Админ-команды
│   │   ├── reminders.py    # Фоновый воркер напоминаний
│   │   ├── health.py       # Механика деградации и смерти выдры
│   │   ├── social.py       # Социальные функции (совместные активности)
│   │   └── stats.py        # Сбор и отображение статистики
│   ├── storage/
│   │   └── json_db.py      # Простое JSON-хранилище
│   ├── assets/
│   │   └── avatars/otter/  # Изображения выдры (пока .txt-заглушки)
│   └── data/               # JSON-файлы с данными (users.json, admin.json, hobbies.json и т.д.)
├── requirements.txt
└── README.md
```

### Технические детали

- **Хранение данных:** JSON-файлы (без реляционных БД)
- **Часовые пояса:** поддержка через `zoneinfo`, по умолчанию Владивосток
- **Статистика:** автоматический сбор метрик, инфографика через `matplotlib`
- **Напоминания:** фоновый воркер, проверка каждую минуту

